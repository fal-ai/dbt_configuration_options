test:
  target: dev-pg #    ***** SQL target
  outputs:
    dev-pg:
      type: postgres
      host: localhost
      port: 5432
      user: pguser
      # ...
      python_engine:
        type: spark
        credentials: ${access_token}
        # ...
